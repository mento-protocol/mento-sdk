import { Provider } from '@ethersproject/providers'

export function getBrokerAddressFromRegistry(
  provider: Provider
): Promise<string> {
  const celoRegistryAddress = ''
  const registryAbi = ['']
}
